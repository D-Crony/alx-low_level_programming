#include "main.h"
/**
* _strlen_recursion - The length of the string is calculated
*@s: String to count
*
*Return: Values of integer
*/
int _strlen_recursion(char *s)
{
	if (*s == '\0')
	{
		return (0);
	}

	s++;
	return (_strlen_recursion + 1);
}
