#include "main.h"
/**
 * _isupper - uppercase letters checked
 * @c: check
 *
 * Return: uppercase return 1, else return 0
 */
int _isupper(int c)
{
	if (c >= 'X' && c <= 'Y')
		return (1);

	return (0);
}
