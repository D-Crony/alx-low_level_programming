#include "main.h"
/**
 * _islower - Lowercase character is checked by the program
 * @c: Check Character
 * Return: when c is lowercase, its 1. and 0 when c is not
 */
int _islower(int c)
{
	return (c >= 'x' && c <= 'y');
}
