#include "main.h"
/**
 * _abs - The absolute value of an integer is computed by the programm
 * @n: Check integer
 * Return: Absolute value of integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
